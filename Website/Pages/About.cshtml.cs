using Microsoft.AspNetCore.Mvc.RazorPages;

namespace MurendeniConstructionArchitects.Pages;

// This page is just static content, so the code-behind doesn't need
// to do anything - OnGet() runs automatically but there's nothing to load.
public class AboutModel : PageModel
{
    public void OnGet()
    {
    }
}
